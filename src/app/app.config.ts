export class AppSettings {
    public static API_ENDPOINT_LOCAL:string='http://localhost:8080/api/';
    public static SIDENAV_CONFIG:any={
        mode: {
            side: "side",
            over: "over",
            push: "push"
        },
        hasBackdrop:{
            value:null // true o false
        }
    }
 }